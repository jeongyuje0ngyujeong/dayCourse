import { RankFunction } from '../types/RankFunction';
export declare class RankSort<T> {
    private readonly rankFunctionList;
    constructor(rankFunction?: RankFunction<T> | RankFunction<T>[]);
    chain(rankFunction: RankFunction<T>): RankSort<T>;
    sort(array: T[]): T[];
    private rankFunction;
}
