import { SearchFunction } from '../types/SearchFunction';
export declare class SearchListIndex<T> {
    private readonly index;
    private readonly searchFunction;
    constructor(searchFunction: SearchFunction<T>);
    run(values: T[], search: string): T[];
    private get;
    private set;
}
