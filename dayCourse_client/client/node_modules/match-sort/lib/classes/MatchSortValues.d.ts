import { SearchFunction } from '../types/SearchFunction';
export declare class MatchSortValues {
    private readonly values;
    private readonly searchFunction;
    private currentResult;
    private lastSearch;
    constructor(sortFn: SearchFunction, list: string[]);
    run(search: string): string[];
}
export declare class MatchSortIndex {
    private readonly index;
    private readonly searchFunction;
    constructor(searchFunction: SearchFunction);
    run(values: string[], search: string): string[];
    private get;
    private set;
}
